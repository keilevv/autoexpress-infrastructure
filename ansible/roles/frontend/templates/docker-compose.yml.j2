version: '3.8'
services:
  frontend:
    build: .
    container_name: frontend
    restart: always
    ports:
      - "80:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
